This webservice provides an endpoint for the data logging programm Prometheus
Setup:
  Clone this repository into the /local folder of your Moodle installation
  Install the Plugin
  Create a user to use the Service and create a Token for him
  Move the client folder of your cloned repository to the machine you run Prometheus on.
  Install a web server like nginx or Apache and host the client.php supplied in the client folder
  Change Domainname, Token and optionally tweak Parameters
  Point Prometheus at the client.php
  Happy Scraping
